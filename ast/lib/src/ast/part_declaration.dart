part of arrow.ast.ast;

class PartDeclaration extends Node {
  const PartDeclaration() : super(const NodeList.empty());
  factory PartDeclaration.parse(Parser parser) {
    throw new UnimplementedError('TODO: PartDeclaration');
  }
}
